sudo: false
language: cpp

addons:
  apt:
    sources:
      - llvm-toolchain-precise-3.8
      - ubuntu-toolchain-r-test
    packages:
      - g++-9
      - clang-6.0

env:
  - COMPILER=g++-9
  - COMPILER=clang++-6.0

script:
  - make CXX=$COMPILER
